# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# The application supports both SQLite and PostgreSQL databases.
# Choose the appropriate DATABASE_URL for your environment:

# Option 1: SQLite (Recommended for Development)
# - No installation required (included with Python)
# - Automatic fallback if PostgreSQL fails
# - File-based database stored locally
# - Perfect for Replit, local development, and testing
DATABASE_URL=sqlite:///./exam_prep_db.db

# Option 2: PostgreSQL (Recommended for Production)
# - Requires PostgreSQL server installation
# - Better performance for concurrent users
# - Advanced features for production use
# Uncomment and configure if using PostgreSQL:
# DATABASE_URL=postgresql://username:password@localhost:5432/studybuddy

# Note: If PostgreSQL connection fails, the application will automatically
# fall back to SQLite to ensure the application starts successfully.

# ============================================================================
# SECURITY CONFIGURATION
# ============================================================================
# Generate a secure random string for production
# You can use: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-this-in-production

# JWT Algorithm (leave as default unless you have specific requirements)
ALGORITHM=HS256

# ============================================================================
# AI/LLM API KEYS
# ============================================================================
# Get your API keys from the respective providers:
# - Gemini: https://makersuite.google.com/app/apikey
# - Mistral: https://console.mistral.ai/
# - Groq: https://console.groq.com/

# Primary AI provider (Gemini)
GEMINI_API_KEY=your-gemini-api-key

# Optional: Additional AI providers for fallback/diversity
MISTRAL_API_KEY=your-mistral-api-key
GROQ_API_KEY=your-groq-api-key
GOOGLE_API_KEY=your-google-api-key

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================
# YouTube API for video recommendations (optional)
# Get from: https://console.cloud.google.com/apis/credentials
YOUTUBE_API_KEY=your-youtube-api-key

# LLM Provider Configuration (optional)
# Comma-separated list of providers to try in order
LLM_PROVIDER_ORDER=gemini,mistral,groq
DEFAULT_LLM_PROVIDER=gemini

# Chatbot Configuration (optional)
CHATBOT_MAX_HISTORY=10
CHATBOT_CONTEXT_LENGTH=4000
